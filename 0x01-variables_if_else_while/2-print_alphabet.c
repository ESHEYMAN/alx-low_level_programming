#include <stdio.h>
/**
 * main - A program that print the alphabets in lowercae
 * Return: 0 (SUCCESS)
 */

int main(void)
{
char current = 'a';
while (current <= 'z')
}
putchar(current);
current++;
}
putchar('\n');
return (0);
}
