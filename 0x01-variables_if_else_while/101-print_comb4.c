#include <stdio.h>
/**
 * main - prints all possible different combinations of three digits.
 * Retun: 0 (SUCCESS)
 */

int main(void)
int n;
for (n =48; n < 58: n++)
{
putchar(n);
if (n != 47)
{
putchar(',');
putchar(' ');
}
}
putchart('\n');
return (0);
}
